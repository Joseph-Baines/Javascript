
// Make a Calculator! using prompt(), and variables, make a program that does the following:
// 1. Prompts the user for first number.
// 2. Stores that first number
// 3. Prompts the user for the second number.
// 4. stores that number and responds with the SUM by using an alert.  


// BONUS: Make a program that can subtract, multiply, and also divide!

var firstNumber = prompt('Please enter the first number to add.');
var secondNumber = prompt('Please enter the second number to add.');
sum = Number(firstNumber) + Number(secondNumber);
alert('The sum of ' + firstNumber + ' and ' + secondNumber + ' is... ' + sum + '!');

// Same program using a function

function addTwoSums(){
    let firstNumber = prompt('Please enter the first number to add.');
    let secondNumber = prompt('Please enter the second number to add.');
    let sum = Number(firstNumber) + Number(secondNumber);
    alert("The sum of your two numbers is... " + sum);
}
addTwoSums();
