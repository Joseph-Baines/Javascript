// Converting strings to numbers

// Declare a string variable from input from the user twice
var first = prompt("Please enter a number to add.");
var second = prompt('Please enter a second number to add');
// Declare a variable consisting of the two strings being converted to numbers and added
var sum = Number(first) + Number(second);
alert('The two numbers added are: ' + sum);
 
